export interface ProjectModel {
    projectName: string;
    projectRepo: string;
    projectUrl?: string;
    projectType: string;
    status: string;
    projectImage: string;
    projectDescription: string;
}
